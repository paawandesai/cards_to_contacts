version: 1.0
runtime: python3
build:
  commands:
    build:
      - echo "Build phase completed"
run:
  pre-run:
    - pip3 install --upgrade pip
    - pip3 install -r requirements.txt
  command: python -m gunicorn --bind 0.0.0.0:8080 --workers 2 app:app
  network:
    port: 8080